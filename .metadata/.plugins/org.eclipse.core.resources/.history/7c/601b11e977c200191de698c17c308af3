%
% fridgeContent.pl
%

content(pizza).
content(pizza).
content(salmon).
content(caviar).
content(pasta).
content(pasta).

listAllContents :- 
	
	
fill(List, NewList) :-
    fact(X),
    not(member(X, List)), !,
    fill([X|List], NewList).
fill(List, List).

%%
fill(NewList) :- fill([], NewList).

showFridgeContent :- 
	output("FRIDGE CONTENT ---------- "),
	showContents,
	output("--------------------------").
		
showContents:- 
 	content( FOOD ),
 	output( content( FOOD ) ),
	fail.
showContents.

output( M ) :- stdout <- println( M ).